{
  "name": "debate-partner",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "tsc -noEmit && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.7",
    "typescript": "^5.0.0"
  }
}
